<h1>Songs Page</h1>
<a href="/">Return Home</a>
<% songs_api.each do |song| %>
  <p>
    <%= p song[1]['name']["name-USen"]  %>
    <img src="<%= song[1]["icon_uri"] %>" alt="">
    <button>Add to wishlist</button>
    <button>Add to collected</button>
  </p>
<%end%>
